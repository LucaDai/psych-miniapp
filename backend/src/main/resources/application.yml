spring:
  application:
    name: psych-miniapp
  profiles:
    active: local

server:
  port: 8080

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: auto
